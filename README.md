Boilerplate for writing new python packages.

# Getting Started

```
git clone https://github.com/gouderm/python-package-templ.git project_name
cd project_name
uv sync --dev
uv run pre-commit install
```
